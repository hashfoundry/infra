{{- if .Values.validatorAlice.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "blockchain.aliceFullname" . }}
  namespace: {{ include "blockchain.namespace" . }}
  labels:
    {{- include "blockchain.aliceLabels" . | nindent 4 }}
spec:
  type: {{ .Values.validatorAlice.service.type }}
  ports:
    - port: {{ .Values.validatorAlice.service.rpcPort }}
      targetPort: rpc
      protocol: TCP
      name: rpc
    - port: {{ .Values.validatorAlice.service.wsPort }}
      targetPort: ws
      protocol: TCP
      name: ws
    - port: {{ .Values.validatorAlice.service.p2pPort }}
      targetPort: p2p
      protocol: TCP
      name: p2p
    - port: {{ .Values.validatorAlice.service.prometheusPort }}
      targetPort: prometheus
      protocol: TCP
      name: prometheus
  selector:
    {{- include "blockchain.aliceSelectorLabels" . | nindent 4 }}
{{- end }}
